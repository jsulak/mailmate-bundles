#!/usr/bin/tclsh

set subject $::env(MM_SUBJECT)
set pattern {Case\s([0-9]+)}
regexp  -nocase $pattern $subject _ bugzid

set domain [exec defaults read com.freron.MailMate FogBugzDomain]

exec open "https://$domain/f/cases/$bugzid/"
